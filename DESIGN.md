# 詳細設計:秘密計算による共同購入ダブルオークション

`CLAUDE.md` から参照される詳細仕様。実装の前提となる用語・アルゴリズムをここにまとめる。

## 1. シナリオ

- 買い手:競合する事業者 B1, B2, B3(例:外食チェーン)
- 売り手:資材メーカー S1, S2
- 対象:単一のコモディティ資材(例:食用油、単位ℓ)

## 2. 価格グリッド

¥95〜¥135、¥5刻みの9段階:`[95, 100, 105, 110, 115, 120, 125, 130, 135]`

## 3. 入力(各参加者の非公開データ)

| 項目 | 買い手 | 売り手 |
|---|---|---|
| 価格しきい値 | 上限価格(これ以下なら買う) | 下限価格(これ以上なら売る) |
| 数量 | 必要量 | 供給可能量 |

## 4. 導出ベクトル(各参加者がローカルで計算)

価格グリッドの9点それぞれについて、その価格で自分が取引する数量を並べた9次元ベクトルを作る。

- 買い手:価格 `p <= しきい値` なら必要量、それ以外は0
- 売り手:価格 `p >= しきい値` なら供給可能量、それ以外は0

## 5. 秘密計算(第1段階:MPC / 加法的秘密分散)

各参加者は、導出ベクトルの各要素 `v` について以下を行う。

1. 乱数 `r1`, `r2` を、法M(想定される全参加者の合計数量の最大値より十分大きい値。例:M = 2^31 − 1)を用いた剰余体 `[0, M)` から一様ランダムに生成する
2. `r3 = (v - r1 - r2) mod M` を計算
3. `r1, r2, r3` をそれぞれノードα・β・γに送付(`(r1 + r2 + r3) mod M = v` が常に成立)

固定区間(例:[-200, 200))からの一様乱数ではなく法M上の剰余演算を採用する理由は、vの大きさによらず各シェアの分布を常に一様にし、情報理論的な秘匿性を担保するため。固定区間だとvが範囲に対して大きい場合や境界付近の値のとき、シェアの分布に偏りが生じvの情報が漏れる可能性がある。

各ノードは、受領した全参加者・全価格点のシェアを、買い手グループ(需要)と売り手グループ(供給)に分けて価格点ごとに局所合算する。3ノード分の局所合算値を最後に合計すると、真の需要合計 `D(p)` ・供給合計 `S(p)` が得られる。

**実装のポイント**:まずは単一プロセス内でノードをシミュレートする版を作り、動作を確認してから、tokio + TCP/gRPCで実際に別プロセス・別ポートに分離する版に格上げする。

## 6. 秘密計算(第2段階:FHE / Zama fhEVM)

各参加者が導出ベクトルをクライアント側でFHE公開鍵を使って暗号化し、コントラクトに送信する。コントラクトは暗号化されたまま `+`(合計)と `>`(閾値比較)を計算する。復号はZamaのしきい値復号ネットワーク(KMS)が担い、最終結果のみが平文になる。

**設計上の注意**:暗号化されたままの比較は加算よりコストが高い。9段階すべてで清算価格を探索すると比較回数が増えるため、価格帯を5段階程度に削減することも検討する。

## 7. 清算価格の決定

価格グリッドを低い順に走査し、`D(p) <= S(p)` を満たす価格のうち最も低いものを清算価格 `p*` とする。該当する価格が存在しなければ「不成立」。

本システムは買い手側(競合事業者)が主体的に発注量を集約する共同購入であるため、「買い手の希望量を可能な限り満額で満たせる、最も安い価格」を優先する設計とした(供給側が希望量を満たせない場合は次節の按分で調整する)。

成立数量 `= min(D(p*), S(p*))`(この定義では常に `D(p*) <= S(p*)` となるため、成立数量 `= D(p*)`)

## 8. 個別割当(按分)

清算価格 `p*` で需要・供給のいずれかに超過がある場合、各参加者の希望数量に比例して配分する。

```
買い手 i の割当 = 希望量_i * (成立数量 / D(p*))
売り手 j の割当 = 希望量_j * (成立数量 / S(p*))
```

この計算は清算価格・成立数量・自分自身の入力値のみで各参加者がローカルに行い、他参加者の値を必要としない。

## 9. 公開される情報 / 非公開のまま残る情報

| 公開される | 非公開のまま |
|---|---|
| 価格点ごとの需要合計・供給合計 | 各参加者の個別しきい値・数量 |
| 清算価格・成立数量 | どの参加者がいくら入力したか |
| 各参加者自身の割当結果(本人にのみ) | 他参加者の割当結果 |

## 10. 段階間の比較軸(発表・レポートで使う)

| | MPC版 | FHE版 |
|---|---|---|
| 計算主体 | 複数ノードに分散 | 単一コントラクト上(暗号化されたまま) |
| 復号 | 全ノードが揃って復元 | しきい値復号ネットワークが代行 |
| 信頼の起点 | 自分たちで用意するノード運営者 | Zamaの分散インフラ |
| オンチェーン性 | オフチェーン | オンチェーン(公開検証可能) |

## 11. スコープ外(今回は実装しない、設計のみ触れる)

- ZKによるAI入力の出所証明 → SP1(zkVM)によるPoCとして一部実装済み(`agent-provenance/`、詳細はspec_v0.4.md §16・`docs/llm_agent_observation.md`§7参照)
- 計算の正しさ自体のSNARK証明(Verifiable FHE)
- 発注実績の事後検証・不正入力対策
- 物流(納品先・納品タイミング)との連携
