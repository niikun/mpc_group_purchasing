# 仕様書

# (プロジェクト名:未定)

## 秘密計算(MPC)による食品業界向け共同購入ダブルオークション・システム

対象材:調味料・消耗品などのコモディティ資材(例:食用油) / 版:v0.4 / 作成日:2026年8月8日

> v0.4での変更点:MPC→FHEの2段階実装ロードマップと、第2段階(Zama fhEVM)のアーキテクチャを追加。プロジェクト名は未定。

---

## 1. 概要

競合関係にある複数の食品・外食事業者が、塩・砂糖・油・紙ナプキン・飲料水などのコモディティ資材を共同購入することで仕入単価を引き下げる仕組みを、秘密計算(Multi-Party Computation, 以下MPC)を用いて実現する。各参加者の発注量・希望価格などの機密情報は、他の参加者にも運営主体にも一切開示されない。開示されるのは、需給が一致する清算価格と成立数量、および各参加者自身の割当結果のみである。

本書は、上記コンセプトを検証するデモシステムの仕様を定める。実運用を想定した本番システムではなく、方式・アルゴリズム・画面構成の妥当性を確認するための実証(PoC)を対象とする。加えてv0.4では、Advanced Cryptography Program(ZK/MPC/FHEを扱う実装講座)の最終成果物として、MPCとFHEの2つの暗号技術で同一システムを実装し比較することを目標に加える。

## 2. 背景と狙い

食品・外食業界における共同購入には、独占禁止法上「参加者間で価格・数量等の競争情報が相互に開示されないよう、適切な秘密保護措置を講じる必要がある」という要請が既に存在する(公正取引委員会ガイドライン)。一方、医療業界のGPO(共同購買機構)のように購買力を高める仕組みは存在するが、いずれも中央の第三者組織が全参加者の発注情報を把握する前提で運営されており、秘匿性を技術的に担保した実装は確認されていない。

本システムは、この空白を埋めるものとして、次を狙いとする。

- 価格協調(カルテル)とは明確に区別される「仕入数量の集約」のみを目的とする
- 中央の第三者に生データを預けることなく、集約結果のみを算出する
- 競合が真に競合機密(発注量・希望価格)を開示せずに協調できることを実証する
- 同一の課題をMPC(第1段階)とFHE(第2段階)の2つの暗号技術で実装し、信頼の起点・オンチェーン性・実装コストのトレードオフを比較する

## 3. 対象範囲(スコープ)

スコープの内外を明確にする。

### 3.1 対象とする商材

品質差がブランド価値に直結しないコモディティ資材を対象とする。

- 対象例:食用油、塩、砂糖、割り箸、紙ナプキン、飲料水・ミネラルウォーター
- 対象外:食肉・鮮魚・青果等の生鮮素材(仕入先・仕入量が競争力の源泉であり、共同化になじまないため対象外とする)

### 3.2 デモの参加者構成

| 区分 | 参加者ID | 役割 |
|---|---|---|
| 買い手(外食チェーン) | B1 / B2 / B3 | 資材を購入する側。上限価格と必要量を入力する |
| 売り手(資材メーカー) | S1 / S2 | 資材を供給する側。下限価格と供給可能量を入力する |

### 3.3 対象商材(デモ版)

デモ版では単一商材(食用油、単位:ℓ)のみを対象とする。複数商材への拡張は「16. 今後の拡張案」で扱う。

## 4. 用語定義

| 用語 | 定義 |
|---|---|
| MPC(秘密計算) | 複数の参加者が、互いの入力を明かさずに、入力に対する関数の計算結果のみを得られる暗号技術群 |
| 加法的秘密分散 | 値vを、合計するとvになる複数のランダムな断片(シェア)に分割する方式。個々のシェアからは元の値を推測できない |
| ノード | 各参加者から送られたシェアを保持・集計する計算主体。本デモでは3ノード構成とする |
| 清算価格 | 供給合計が需要合計を初めて満たす(下回らなくなる)価格。ダブルオークションにおける約定価格 |
| 按分(比例配分) | 清算価格において需要または供給に超過がある場合、各参加者の希望数量に比例して成立数量を配分する処理 |
| AI入札エージェント | 参加者ごとに独立して動作し、自社の内部データのみを参照して入札(しきい値・数量)を提案するオプション機能。提案の反映には本人の確認操作を要する |
| FHE(準同型暗号) | 暗号化されたままのデータに対して演算(加算・乗算・比較等)を行える暗号方式。復号して初めて演算結果が平文になる |
| fhEVM | FHEを用いてイーサリアム互換のスマートコントラクト上で暗号化されたまま計算を行える基盤(Zama社)。本書ではその実装であるZama fhEVMを指す |
| しきい値復号(KMS) | 復号鍵を複数のノードで分散管理し、一定数以上のノードが合意した場合のみ復号を許可する仕組み。Zama fhEVMでは分散型のKey Management Serviceがこれを担う |

## 5. 実装ロードマップ:MPCとFHEの2段階構成

本プロジェクトは、同一のダブルオークション・ロジックを2つの暗号技術で実装する。Advanced Cryptography Program(全7週、ZK/MPC/FHEを扱う実装講座)の進行に合わせて段階的に実装する。

| 週 | 講座テーマ | 本プロジェクトでの作業 |
|---|---|---|
| Week 1 | Programmable Cryptography概観 | 開発環境構築(Rust、fhEVM Hardhatテンプレート) |
| Week 2 | MPC(秘密分散・OT・Garbled Circuits) | 第1段階:Rustで加法的秘密分散を実装 |
| Week 3 | ZKP I(コミットメント・算術化) | (第1段階の実装を継続。ZKPは将来拡張として設計のみ) |
| Week 4 | ZKP II(SNARK/STARK/GKR) | 第1段階:複数プロセス間の実通信化(tokio) |
| Week 5 | FHE(格子暗号・LWE) | 第2段階:fhEVMで基本演算(加算・比較)を実装 |
| Week 6 | 実装応用(Permutation Argument等) | 第2段階:清算価格ロジックの実装 |
| Week 7 | Advanced Topics & Demo | 両段階の比較検証、デモ発表 |

### 5.1 第1段階(MPC)と第2段階(FHE)の比較軸

| 観点 | 第1段階:MPC | 第2段階:FHE |
|---|---|---|
| 計算主体 | 複数ノードに分散(誰かが運営する必要) | 単一のスマートコントラクト上(暗号化されたまま計算) |
| 復号 | 全ノードが揃って初めて復元 | しきい値復号ネットワーク(Zama KMS)が代行 |
| 信頼の起点 | 自分たちで用意するノード運営者 | Zamaの分散インフラ(現状13ノード) |
| 実装言語 | Rust | Solidity(コントラクト)+ TypeScript(クライアント) |
| オンチェーン性 | オフチェーン | オンチェーン(公開検証可能) |
| 実装の重さ | 比較的軽い(足し算ベース) | 比較演算の回数が増えるとコストが増大 |

## 6. システム全体像(第1段階:MPC)

本デモは単一画面内で全参加者・全ノードの役割をシミュレートする。各参加者の入力は画面遷移(タブ切替)で分離され、他タブの入力値は表示されない。実装が進んだ段階では、Rustで複数プロセス・複数ノードによる実通信に置き換える(5.1節参照)。

```
[買い手 B1, B2, B3]    [売り手 S1, S2]
        ↓ 入力(非公開)
各参加者ローカルで価格点ごとの数量ベクトルを生成
        ↓ 加法的秘密分散(3シェア、法M上の剰余演算)
[ノードα]  [ノードβ]  [ノードγ]  ← シェアを集計(単独では無意味な値)
        ↓ 3ノード分を合算
価格点ごとの需要合計 D(p) ・供給合計 S(p) を復元
        ↓
清算価格 p* と成立数量を決定 → 各参加者へ個別結果を返却
```

## 7. データ仕様

### 7.1 価格グリッド

¥95〜¥135/ℓ、¥5刻みの9段階(¥95, 100, 105, 110, 115, 120, 125, 130, 135)。

### 7.2 参加者入力(非公開データ)

| 項目 | 買い手 | 売り手 | 型 |
|---|---|---|---|
| 価格しきい値 | 上限価格(これ以下なら買う) | 下限価格(これ以上なら売る) | 価格グリッドから1点選択 |
| 数量 | 必要量(ℓ) | 供給可能量(ℓ) | 0以上の整数、50刻み推奨 |

### 7.3 導出データ(参加者ローカルで計算、外部非公開)

各参加者は、価格グリッドの9点それぞれについて、自分がその価格で取引する数量を表す9次元ベクトルをローカルで生成する。

- 買い手:価格 p ≤ しきい値 の場合は必要量、それ以外は0
- 売り手:価格 p ≥ しきい値 の場合は供給可能量、それ以外は0

### 7.4 公開データ(計算完了後に全参加者へ開示)

- 価格点ごとの需要合計 D(p) ・供給合計 S(p)(9点分のグラフ)
- 清算価格 p*
- 成立数量(= min(D(p*), S(p*)))

### 7.5 個別データ(本人にのみ開示)

- 自分が成立に含まれるか否か
- 按分後の自分の割当数量

## 8. 処理フロー(第1段階:MPC / 秘密分散)

### 8.1 秘密分散(シェア生成)

各参加者は、導出ベクトルの各要素vについて、以下の手順で3分割する。

1. 乱数r1, r2を、法M(想定される全参加者の合計数量の最大値より十分大きい値。例:M = 2^31 − 1 などの素数)を用いた剰余体[0, M)から一様ランダムに生成する
2. r3 = (v − r1 − r2) mod M を計算する
3. r1, r2, r3をそれぞれノードα、β、γに送付する((r1 + r2 + r3) mod M = v が常に成立し、各シェアはvの値によらず[0, M)上で一様に分布するため、個々の値からはvの情報が一切漏れない)

*固定範囲の実数(例:[-200, 200))ではなく法Mを用いた剰余演算を採用する理由は、vの大きさによらず各シェアの分布を常に一様にし、情報理論的な秘匿性を担保するためである(固定範囲の乱数だと、vが範囲に対して大きい場合や境界付近の値のとき、シェアの分布に偏りが生じvの情報が漏れる可能性がある)。またMは、個々の参加者の数量ではなく、8.2節で複数参加者分を合算した後の最大値(想定される全参加者の合計発注量)を上回るように設定する必要がある。*

### 8.2 ノード集計

各ノードは、受領した全参加者・全価格点のシェアを、買い手グループ(需要)と売り手グループ(供給)に分けて価格点ごとに局所合算する。この時点でのノード内の値は、他の参加者の値と混ざり合っており、単独では意味を持たない。

### 8.3 復元(合算)

3ノード分の局所合算値を価格点ごとに合計し、真の需要合計D(p)・供給合計S(p)を得る。

### 8.4 清算価格の決定

価格グリッドを価格の低い順に走査し、D(p) ≤ S(p) を満たす価格のうち最も低いものを清算価格p*とする。該当する価格が存在しない場合は「不成立」とする。

本システムは買い手側(競合事業者)が主体的に発注量を集約する共同購入であるため、「買い手の希望量を可能な限り満額で満たせる、最も安い価格」を優先する設計とした(供給側が希望量を満たせない場合は8.5節の按分で調整する)。

成立数量 = min(D(p*), S(p*))(この定義では常に D(p*) ≤ S(p*) となるため、成立数量 = D(p*))

### 8.5 個別割当(按分)

清算価格p*において、需要側・供給側のいずれかに超過がある場合、各参加者の希望数量に比例して成立数量を配分する。

```
買い手iの割当 = 希望量_i × (成立数量 ÷ D(p*))
売り手jの割当 = 希望量_j × (成立数量 ÷ S(p*))
```

この計算は、清算価格・成立数量・自分自身の入力値のみを用いて各参加者のローカル環境で行われ、他参加者の入力値を必要としない。

## 9. 処理フロー(第2段階:FHE / Zama fhEVM)

第1段階と同一のダブルオークション・ロジックを、Zama fhEVM上のスマートコントラクトとして実装する。ノードによる秘密分散の代わりに、準同型暗号による暗号化状態での直接計算を用いる。

### 9.1 暗号化と送信

各参加者は、7.3節の導出ベクトルをクライアント側(TypeScript、`@zama-fhe/relayer-sdk`)でFHE公開鍵を用いて暗号化し、スマートコントラクトにトランザクションとして送信する。

### 9.2 オンチェーン計算

コントラクトは、euint型(暗号化整数)に対して直接、加算(`+`)と比較(`>`)を実行する。計算の途中、平文の値はコントラクト内にも運営者にも一度も現れない。

### 9.3 しきい値復号

清算価格・成立数量・各参加者の割当結果は、Zama KMSによるしきい値復号(現行仕様では複数ノードの合意を要する)を経て、開示すべき範囲にのみ復号される。第1段階における「ノード運営者を誰が務めるか」という課題は、この分散インフラの利用により解消される。

### 9.4 実装上の注意

- 暗号化されたままの比較演算は加算より処理コストが高い。9段階すべてで清算価格を探索すると比較回数が増えるため、価格帯を5段階程度に削減することも検討する
- 2026年時点のfhEVMのスループットは向上途上であり、少人数のデモ規模では実用的だが、大規模運用には別途検証が必要
- スマートコントラクトの実装にはZama公式のBlindAuctionサンプル(fhevmリポジトリ)のパターンを参考にする

## 10. 画面仕様

| 画面 | 内容 |
|---|---|
| 参加者タブ(B1〜S2) | 各参加者の入札スリップ(価格しきい値の選択、数量入力)。他タブの入力値は表示されない |
| 清算タブ | 秘密計算の実行ボタン、シェア分散/ノード集計/復元の3段階の可視化、需要・供給曲線グラフ、清算価格と成立数量の表示 |
| 結果表示(各参加者タブ内) | 計算完了後、当該参加者にのみ「清算価格・成立可否・割当数量」を表示 |

## 11. AI入札エージェント(オプション機能)

各参加者は、価格しきい値・数量の入力を手動で行う代わりに、自社専用のAI入札エージェントに提案を作成させることができる。エージェントは自社の内部データのみを参照し、提案は自社が確認・確定するまで入札には反映されない。第1段階・第2段階いずれの実装にも独立して追加できる、コア機能とは分離されたモジュールと位置づける。

### 11.1 目的

- 在庫・使用量実績など社内データに基づく入札の質向上と、入力作業の省力化
- 複数商材・高頻度開催へ拡張した際の運用負荷の軽減(将来の拡張を見据えた布石)

### 11.2 対象データ(自社内のみ・非公開)

| 区分 | 参照するデータ例 |
|---|---|
| 買い手(B1〜B3) | 直近の月間使用量、現在庫日数、社内方針(価格上限・優先順位)、特記事項 |
| 売り手(S1・S2) | 生産余力、社内方針(下限価格・数量方針)、特記事項 |

### 11.3 処理フロー

1. 参加者が「AIエージェントに入札を提案させる」を実行する
2. 当該参加者1社分の内部データのみを含むリクエストを、その場で1回限り生成しモデルに送付する(セッション・会話履歴は保持しない)
3. モデルは価格グリッド9点の中から、しきい値・数量・提案理由(短文)を返す
4. 参加者はその場で提案内容を確認し、「反映する」を押した場合のみ入札欄に反映される。反映前に数値を手動で修正することも可能

### 11.4 データ分離設計

- 1回のモデル呼び出しに含まれる入力は、常に1社分の内部データのみとする
- 参加者間でモデルの呼び出しセッション・会話履歴・学習データを共有しない
- 運営主体・ベンダーが複数参加者の入力を横断的に集約・学習に利用しない

### 11.5 独禁法上の安全策(RealPage事件を踏まえた設計)

米国では、複数の競合が同一ベンダーの価格推奨アルゴリズムに非公開の競争機微情報を提供し、ベンダーがそれを集約して価格提案を生成していた事案が、ハブ・アンド・スポーク型の共謀として独占禁止法違反と認定された(詳細は17章参照)。本システムは、次の設計でこの構図を回避する。

- 各社のAIエージェントは独立した呼び出しとして実行し、他社の入力・過去の入札傾向にアクセスできない構成とする
- 最終的な入札の意思決定は、AIの提案ではなく参加者自身の確定操作に置く(完全自動化・自動発注は行わない)
- モデルの学習・ファインチューニングに複数参加者の非公開データを混在させない(11.6節・14章参照)

### 11.6 本機能の限界

- 提案内容の妥当性(自社の利益になっているか)そのものの監査は本デモの対象外(15章参照)
- 入力データが本当に自社データのみで構成されていたことの暗号学的な証明は行っていない(将来の拡張案は16章参照)
- モデルの学習データに他社の情報が含まれていないことの技術的な検証は、現状は契約・ベンダー監査に依存する

## 12. 非機能要件・前提

- 第1段階(MPC)のデモは単一プロセス内で全参加者・全ノードをシミュレートする版から始め、tokio等による複数プロセス間の実通信版へ段階的に移行する
- シェア生成に用いる乱数は、剰余体[0, M)から一様ランダムに生成する(8.1節)。デモの初期段階では暗号学的乱数ではなく擬似乱数で代替してよい
- 参加者数は買い手3・売り手2の固定構成とし、可変人数への対応はスコープ外とする
- 第2段階(FHE)は、fhEVM対応チェーン(Zamaのテストネット等)へのネットワークアクセスを要する
- AI入札エージェントの呼び出しは、外部モデルAPIへのネットワークアクセスを要する(11章参照)

## 13. セキュリティ設計上の要点

- 第1段階:ノードは相互に独立した運営主体(例:業界団体・監査法人・クラウド事業者など)が担い、単独のノードが全シェアを保持しない構成とする
- 第1段階:ノード間通信は暗号化し、各参加者は自分のシェア送付先ノードの正当性を検証できるようにする
- 第2段階:ノード運営者を自前で用意する必要がなく、Zamaの分散型KMSがしきい値復号を代行する。これにより第1段階で残る「誰がノードを運営するか」という信頼の課題が構造的に軽減される
- 参加者が実際にその数量を発注したかどうかの検証(不正入力対策)は本デモでは扱わない。将来的にはゼロ知識証明等による発注実績の検証を拡張として検討する(16章参照)

## 14. 法務上の留意事項

公正取引委員会のガイドラインでは、共同購入において参加者間で価格・数量等の重要な競争情報が相互に開示されないよう、適切な秘密保護措置を講じる必要があるとされている。本システムはこの要請に技術的に応えるものだが、以下は法務確認が別途必要な論点である。

- 運営主体(ノードの委託先、またはfhEVMのしきい値復号ネットワーク)が、参加者のシェア以外の経路(例:物流実績)から間接的に発注量を推定できないか
- 参加者の市場シェア合計が高い場合、共同購入自体が独禁法上の問題となりうる点は、MPC/FHEの秘匿性とは別に検討が必要
- 業界・地域により適用法令が異なるため、対象市場ごとに法務レビューを行う
- AI入札エージェントを採用する場合、米国RealPage事件で問題視されたハブ・アンド・スポーク型の構図に該当しないよう、参加者ごとにベンダー・モデル・学習データを分離する(11.5節参照)

## 15. 制限事項(デモ版のスコープ外)

- 複数商材の同時発注・商材間の代替関係の考慮
- 発注実績の事後検証・不正入力対策
- 物流(納品先・納品タイミング)との連携
- 参加者数の動的な増減への対応
- 本番運用を想定した認証・アクセス制御
- AI入札エージェントが提案した内容の妥当性そのものの検証(提案が自社の利益に反していないかの監査)
- 第1段階(MPC)の`coordinator`はシェア配布と3ノード分の最終集計(復元)の両方を担う単一プロセスであり、可用性上の単一障害点(SPOF)になっている。加法的秘密分散はシェアが1つでも欠けると情報が一切漏れない性質があるため秘匿性上の懸念は小さいが、`coordinator`が停止するとオークション全体が進行不能になる。ノードの物理的分離(16章)と合わせ、集計役の冗長化・分散化は将来課題とする
- 第2段階(FHE)は`tfhe-rs`(Rust)による実装とし、当初想定していたfhEVM(Solidity+オンチェーン)は採用しない。そのため、オンチェーン性の検証(ガス代・スループット等)は行わない。また、Zamaのしきい値復号KMS(13ノード)は利用せず、復号鍵の管理は第1段階の3ノード加法的分散と同様に簡略化する(実運用ではしきい値復号KMSの利用を前提とする)

## 16. 今後の拡張案

- 複数商材(調味料・消耗品カテゴリ全般)への対応
- 固定人数から可変人数の参加者集合への一般化
- ノードの物理的分離(業界団体・第三者クラウド等への実配置、第1段階)
- ゼロ知識証明による発注実績の検証機能
- 独禁法チェックリストの自動診断機能(市場シェア集中度の警告等)
- AI入札エージェントへの入力データの出所をゼロ知識証明で検証する機能(モデル推論自体の証明ではなく、入力が自社データのみで構成されていることの証明に限定するのが現実的)
- AIモデルの学習データに他社データが混入していないことの検証(verifiable training)は現時点で研究段階であり、当面は契約・監査ベースでの担保が必要
- 計算の正しさをSNARKで証明するVerifiable FHEへの発展(Zamaが2025年11月に買収したKakarotの技術方向性と関連)

## 17. 参考事例

- デンマーク砂糖大根オークション(2008年〜):世界初の商用MPC実装。生産者(農家)と単一買い手(Danisco社)の間で、入札を秘匿したままダブルオークションの清算価格を算出。2023年時点で稼働20年以上
- KDDIデータクリーンルーム(2024年3月商用開始):複数企業の顧客データを秘密計算で秘匿したまま統合・分析する広告領域の実装例
- ボストン女性労働評議会(2015年〜):200社超の給与データをMPCで秘匿したまま男女賃金格差を集計
- GPO(共同購買機構、医療業界):購買力の集約による価格引き下げの仕組み自体は確立しているが、中央組織が発注情報を把握する前提であり、秘密計算による秘匿は行われていない
- RealPage事件(米国、2024年提訴〜2025年DOJ和解):複数の競合が同一ベンダーの価格推奨アルゴリズムに非公開データを提供し合った結果、ハブ・アンド・スポーク型の共謀と認定された。本システムのAI入札エージェント分離設計(11章)は、この構図を避けることを直接の目的とする
- Zama fhEVM:準同型暗号(FHE)を用いてEVM互換のスマートコントラクト上で暗号化されたまま計算を行う基盤。公式サンプルにブラインドオークション(入札額・落札者を明かさない)が含まれ、本システムの第2段階(9章)の直接の実装土台となる
